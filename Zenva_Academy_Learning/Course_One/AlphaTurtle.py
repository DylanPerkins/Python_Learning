# Imports everything from the library, turtle
from turtle import *

# Moves forward x pixels
forward(50)
# Move to the right, x degrees
right(90)
forward(50)
right(90)
forward(50)
right(90)
forward(50)

# Keeps window open after above code has finished running
done() 